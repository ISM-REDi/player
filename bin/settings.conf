# settings
LABEL="evalunit"
DATA_DIR="datas/evalunit/"
SAVE_DIR="results/evalunit/"
PARQUET_EMBEDDING=$DATA_DIR"filename.parquet"
RANDOM_SAMPLING_LEN=100
INDEXCOL="corpusId" # If need.

# === WARNING: Do not modify the following section ===
# This part is automatically managed and changes may cause unexpected behavior.
SAVE_DIR1=$SAVE_DIR"1/"
SAVE_DIR2=$SAVE_DIR"2/"
SAVE_DIR3=$SAVE_DIR"3/"

function cat_settings () {
  echo "LABEL="$LABEL
  echo "DATA_DIR="$DATA_DIR
  echo "SAVE_DIR="$SAVE_DIR
  echo "PARQUET_EMBEDDING="$PARQUET_EMBEDDING
  echo "RANDOM_SAMPLING_LEN="$RANDOM_SAMPLING_LEN
}